{
  "name": "email-relay",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm -r run dev",
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint",
    "db:generate": "pnpm --filter @email-relay/database db:generate",
    "db:push": "pnpm --filter @email-relay/database db:push",
    "db:seed": "pnpm --filter @email-relay/database db:seed",
    "db:studio": "pnpm --filter @email-relay/database db:studio"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "packageManager": "pnpm@11.3.0"
}
